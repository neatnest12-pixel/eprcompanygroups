{
  "name": "eprcompanygroups-root",
  "private": true,
  "scripts": {
    "build": "npm --prefix main/frontend install --no-audit --no-fund && npm --prefix main/frontend run build && node -e \"const fs=require('fs'); fs.rmSync('dist',{recursive:true,force:true}); fs.cpSync('main/frontend/dist','dist',{recursive:true});\""
  }
}
